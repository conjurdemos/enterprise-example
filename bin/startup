#!/bin/bash -e

demo_name=$1
if [ -z "$demo_name" ]; then
  echo demo_name is not specified
  exit 1
fi

shift

export DEMO_NAME=$demo_name
summon -f secrets.dev.yml docker-compose up -d

docker exec -it enterpriseexample_admin_1 ./launch.sh
